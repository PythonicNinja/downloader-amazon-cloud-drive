============
Installation
============

At the command line::

    $ easy_install downloader-amazon-cloud-drive

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv downloader-amazon-cloud-drive
    $ pip install downloader-amazon-cloud-drive
